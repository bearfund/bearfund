/**
 * Global test setup
 *
 * This file runs before all tests.
 */

import { afterEach } from 'vitest';

// Clear all mocks after each test to prevent test pollution
afterEach(() => {
  // This is automatically done by vitest, but being explicit
});
